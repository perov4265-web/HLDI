# Обязательные зависимости
PySide6>=6.5
pyserial>=3.5
numpy>=1.21

# Необязательные зависимости (можно не ставить):
#   opencv-python — нужен только для камеры совмещения
#   bleak         — нужен только для связи по Bluetooth (BLE)
opencv-python>=4.5
bleak>=0.21
